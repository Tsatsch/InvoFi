// package.json
{
    "name": "invo-fi",
    "version": "1.0.0",
    // ... other fields ...
    "scripts": {
      // This script in package.json can be used for running tests directly with yarn/npm
      "test": "ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
    },
    "dependencies": {
      "@coral-xyz/anchor": "^0.30.1" 
      // Other runtime dependencies if you have them
    },
    "devDependencies": {
      "@types/chai": "^5.2.2",
      "@types/mocha": "^10.0.10",
      "@types/node": "^20.11.0", // Add or ensure this is present for Node.js types
      "chai": "^5.2.0",
      "mocha": "^11.0.0", 
      "ts-mocha": "^11.1.0",
      "ts-node": "^10.9.2",
      "typescript": "^5.8.3"
    }
  }